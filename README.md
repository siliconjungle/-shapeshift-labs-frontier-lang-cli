# @shapeshift-labs/frontier-lang-cli

Command line interface for parsing, checking, hashing, and emitting Frontier Lang projects.

## Install

```sh
npm install -g @shapeshift-labs/frontier-lang-cli
frontier-lang check examples/todo.frontier
```

Commands: `parse`, `check`, `hash`, and `emit-ts`.

## License

MIT.
